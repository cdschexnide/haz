/* 

function getHazardousMaterialDetails(unNumber: string, materialsMap: HazardousMaterialMap): HazardousMaterial | string:
    material = materialsMap.get(unNumber)

    if !material:
        return `Material with UN ${unNumber} not found.`
    
    return material


*/
